# This script is stored as a GitHub Secret (SECRET_TRANSLATE_GEMINI_PY)
# and restored at runtime by the workflow. Not stored in this repository.
raise RuntimeError("This script must be restored from GitHub Secrets before running.")
